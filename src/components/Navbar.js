import React from "react"

const Navbar = () => {
  const headerStyle = {
    padding: "20px 0",
    lineHeight: "1.5em",
  }

  return (
    <header style={headerStyle}>
       test
    </header>
  )
}

export default Navbar